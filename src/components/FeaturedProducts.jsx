const FeaturedProducts = () => {
    // { products }
    // const featured = products.slice(0, 4); // First 4 as featured

    return (
        <div className="mt-12 flex justify-center flex-col items-center gap-10 pb-25">
            <h2 className="text-2xl font-bold text-pink-600 mb-6">Featured Products</h2>
            {/* <ProductGrid products={featured} /> */}
            this is the hot or special or sales products
        </div>
    );
};
export default FeaturedProducts;